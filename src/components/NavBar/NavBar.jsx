import './NavBar.css'
import React from 'react'

const NavBar = () => {
  return (
    <nav>
        <a href="#">LA ASOCIACIÓN</a>
        <a href="#">COLABORA</a>
        <a href="#">CONTACTO</a>
        <a href="#">DONAR</a>
    </nav>
  )
}

export default NavBar